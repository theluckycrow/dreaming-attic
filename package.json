{
  "name": "dreaming-attic-mcp",
  "version": "1.0.0",
  "description": "An MCP server for The Dreaming Attic — a persistent space for half-formed things, dream logic, and threads worth pulling.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@upstash/redis": "^1.28.0",
    "express": "^4.18.2",
    "zod": "^3.22.0"
  }
}
